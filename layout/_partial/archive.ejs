<% if (index) { %>
  <div class="container">
    <main class="main">
      <setion class="article-list">
        <% page.posts.each(function (post) { %>
          <%- partial('article', { post: post, index: true }) %>         
        <% }) %>
      </section>
    </main>
  </div>
<% } %>
